import { Modal } from '../components'
import ModalExamples from './ModalExamples'

import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks"

<Meta title="Components|Modal" component={Modal} />

# Modal

```js
import { Modal } from "txstate-react/lib/components"
```

## Props

---

If either `value` or `onChange` are provided, both must be provided. These props turn the picker into a controlled component.

<Props of={Modal} />

<Preview>
  <Story name="basic">
    <ModalExamples />
  </Story>
</Preview>
