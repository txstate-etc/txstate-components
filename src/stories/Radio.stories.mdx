import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks'
import { Radio, Stack, Button, RadioGroup, Form } from '../components'

<Meta title='Components/Radio' component={Radio} />

# Radio
```typescript
import { Radio } from '@txstate-mws/txstate-react/Radio'
```

## Props
<Props of={Radio} />

### Selected
<Preview>
  <Story name='selected'>
    <Form
      initialValues={{
        sides: 'chianti'
      }}
      onChange={({form}) => console.log('Form', form)}
    >
      <RadioGroup group='sides' label='Sides for guest'>
        <Stack spacing={12} style={{ padding: '12px 0' }}>
          <Radio label='Fava Beans' id='fava-beans' />
          <Radio label='Chianti' id='chianti' />
        </Stack>
      </RadioGroup>
    </Form>
  </Story>
</Preview>

