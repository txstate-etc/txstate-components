import React from 'react'
import EmailEditor from 'react-email-editor'

export const EmailTemplate = () => {
  return (
    <div style={{ position: 'absolute', top: 0, bottom: 0, width: '100%' }}>
      <EmailEditor minHeight='100%' />)
    </div>
  )
}
